{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Read(//{{ workspace.root }}/**)",
      "Write(//{{ workspace.root }}/**)"
    ]
  },
  "metadata": {
    "daily_driver_version": "{{ workspace.version }}"
  }
}
