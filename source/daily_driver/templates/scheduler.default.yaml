# Read at `install-scheduler` invocation time to supply scheduling defaults.
# Never copied into the user workspace — lives in the installed package only.
checkin:
  times: ["11:00", "15:00"]
jobs:
  time: "07:00"
